name: Main Workflow

on:
  push:
    branches:
      - main
      - develop
      - "feature/*"

permissions:
  id-token: write
  contents: read

jobs:
  deploy-dev:
    name: Deploy to Dev
    uses: ./.github/workflows/services-deploy.yml
    with:
      environment: dev
      aws_account_id: "474668415523"
      lambda_functions: >-
        [
          {"name": "get-student-data", "directory": "lambda/worker/GetStudentDetails"}
          {"name": "get-student-courses", "directory": "lambda/worker/GetStudentCourses"},
          {"name": "get-program-details", "directory": "lambda/worker/GetProgramDetails"},
          {"name": "hello_world", "directory": "lambda/HelloWorld"},
          {"name": "student-query-answer-generator", "directory": "lambda/query/AnswerGenerator"},
          {"name": "student-query-intake", "directory": "lambda/query/QueryIntake"},
          {"name": "student-query-llm-analyzer", "directory": "lambda/query/LLMQueryAnalyzer"},
          {"name": "student-query-worker-dispatcher", "directory": "lambda/query/WorkerDispatcher"},
          {"name": "student-querry-response-aggregator", "directory": "lambda/query/ResponseAggregator"},
          {"name": "student-querry-orchestrator", "directory": "lambda/query/Orchestrator"}
        ]

  deploy-prod:
    name: Deploy to Prod
    needs: deploy-dev
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
    uses: ./.github/workflows/services-deploy.yml
    with:
      environment: prod
      aws_account_id: "054037129431"
      lambda_functions: >-
        [
          {"name": "get-program-details", "directory": "lambda/GetProgramDetails"},
          {"name": "hello_world", "directory": "lambda/HelloWorld"}
        ]
